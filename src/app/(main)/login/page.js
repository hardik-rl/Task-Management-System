export const metadata = {
  title: "Task Management System | Login",
  description: "Login to your Task Management System account.",
};

import LoginClient from "./LoginClient";

export default function LoginPage() {
  return <LoginClient />;
}
